create table local_constraints (
    id int primary key,
    username varchar(100) unique,
    school_id int references school on delete cascade on update set null,
    country_id serial references country (id) on update set default /* Le saque la coma, no se puede tener coma terminal */
);