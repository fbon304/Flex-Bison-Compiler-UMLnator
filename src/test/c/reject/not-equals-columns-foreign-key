-- FOREIGN KEY con cantidad de columnas distinta
CREATE TABLE parent3 (
    a INTEGER,
    b INTEGER,
    PRIMARY KEY (a, b)
);

CREATE TABLE child3 (
    a INTEGER,
    FOREIGN KEY (a) REFERENCES parent3(a, b)
);