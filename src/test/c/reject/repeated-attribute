create table table_name (
    edad int default 18,
    birthdate timestamp default current_timestamp, /* no se puede tener funciones sin la keyword default, se la agregue */
    birthdate timestamp default localtimestamp
);