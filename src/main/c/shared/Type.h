#ifndef TYPE_HEADER
#define TYPE_HEADER

typedef enum {
	false = 0,
	true = 1
} boolean;

typedef int Token;

typedef enum {
	INTEGER_TYPE, 
	SMALLINT_TYPE,
	BIGINT_TYPE,
	REAL_TYPE,
	DOUBLE_TYPE,
	BOOLEAN_TYPE,
	FLOAT_TYPE,
	DATE_TYPE,
	TIMESTAMP_TYPE,
	INTERVAL_TYPE,
	TEXT_TYPE,
	SERIAL_TYPE_TYPE,
	CHAR_TYPE,
	VARCHAR_TYPE,
	TIME_TYPE,
	NUMBER_TYPE,
	BOTTOM
} DataType;

typedef enum {
	PRIMARY_KEY_TYPE,
	UNIQUE_TYPE,
	FOREIGN_KEY_TYPE,
	NONE
} ConstarintDataType;

#endif